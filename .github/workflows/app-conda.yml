name: Run tests using Conda

on: [push]

jobs:
  build-linux:
    runs-on: ubuntu-latest
    strategy:
      max-parallel: 5
      matrix:
        python-version: ["3.8", "3.9", "3.10"]
    steps:
    - uses: actions/checkout@v4

    - name: Setup Miniconda
      uses: conda-incubator/setup-miniconda@v3
      with:
        python-version: ${{ matrix.python-version }}
        auto-update-conda: true
        # Use mamba for faster operations, requires conda-forge channel
        use-mamba: true 
        channels: conda-forge,bioconda,defaults

    - name: Install dependencies
      run: |
        conda env update --file environment.yml --name qapa
        conda install --name qapa -c conda-forge pytest

    - name: Install ruff without running check or format
      uses: astral-sh/ruff-action@v3
      with:
        version: ">=0.15.7,<0.16.0"
        src: >-
          qapa
          tests/python

    - name: Test with pytest
      run: |
        conda run -n qapa pytest tests/python

    - name: Test R
      run: |
        cd tests
        conda run -n qapa Rscript run_R_tests.R   
